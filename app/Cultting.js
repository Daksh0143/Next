import { Grid, Typography } from '@mui/material'
import React from 'react'

const Cultting = ({handleClick}) => {
  return (
    <Grid onClick={handleClick}>
        <Typography variant='h6'>I am Daxesh Chauhhan</Typography>
    </Grid>
  )
}

export default Cultting